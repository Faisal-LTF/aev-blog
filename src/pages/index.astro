---
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { getAllPosts, sortMDByDate } from "@/utils";
import { Fragment } from "astro/jsx-runtime";

const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const cactusTech: Array<{ title: string; desc: string; href: string }> = [
	{
		title: "Melati (UMKM)",
		desc: "Landing pages untuk UMKM, untuk tempat promosi usaha kue kering.",
		href: "https://",
	},
	{
		title: "Sasirangan",
		desc: "Website untuk  promosi pelatihan dan pembuatan kerajinan sasirangan.",
		href: "https://",
	},
];
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">Fuck!!! World!</h1>
		<p class="mb-4">
			Hi,I'm Faisal. <br /> This is my personal blog site, where I share things that interest me.
			<br />
			 However, if you're interested in collaborating with me to discuss and create interesting
			applications for business or personal use, click the icon below.
		</p>
		<SocialList />
	</section>
	<section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
	<section class="mt-16">
		<h2 class="title mb-4 text-xl">Recent Projects</h2>
		<p class="mb-4">COMING SOON ..</p>
		<!-- <ul class="space-y-4 sm:space-y-2">
			{
				cactusTech.map(({ href, title, desc }) => (
					<li>
						<a
							href={href}
							target="_blank"
							rel="noopener noreferrer"
							class="cactus-link inline-block"
						>
							{title}
						</a>
						:<p class="inline-block sm:mt-2">{desc}</p>
					</li>
				))
			}
		</ul> -->
	</section>
</PageLayout>
